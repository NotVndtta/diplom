<div class="max-h-screen bg-white flex items-center justify-center px-4 py-8 sm:px-6 lg:px-8  rounded-3xl">
  <div class="max-w-md w-full space-y-8 ">
    <div>
      <h2 class="text-center text-3xl font-extrabold text-gray-900">Забыли пароль?</h2>
    </div>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "mt-8 space-y-6" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="rounded-md -space-y-px">
        <div class="mb-6">
          <%= f.label :email, class: "block text-sm font-medium text-gray-700" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-lime-500 focus:border-lime-500 sm:text-sm" %>
        </div>
      </div>

      <div>
        <%= f.submit "Отправить инструкцию по восстановлению", class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-lime-600 hover:bg-lime-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-lime-500" %>
      </div>
    <% end %>

    <div class="text-sm text-center text-gray-600">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
