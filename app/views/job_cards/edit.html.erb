<% content_for :title, "Редактирование карточки" %>

<div class="min-h-screen flex items-center justify-center px-4 py-4">
  <div class="w-full max-w-2xl bg-white p-6 rounded-3xl shadow-lg">
    <div class="flex justify-end mb-4">
      <%= link_to "Просмотр карточки", @job_card, class: "rounded-md px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-800 font-medium transition" %>
    </div>

    <h1 class="font-bold text-3xl text-center pb-6">Редактирование агрооперации</h1>

    <%= render "form", job_card: @job_card %>
  </div>
</div>
