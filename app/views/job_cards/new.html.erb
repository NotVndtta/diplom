<div>
  <%= link_to "← Назад к вакансиям", job_cards_path, class: "rounded-md px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-800 font-medium transition" %>
</div>

<% content_for :title, "New job card" %>

<div class="min-h-screen flex items-center justify-center px-4 py-4">
  <div class="w-full max-w-2xl">
    <h1 class="font-bold text-3xl text-center mb-6">Новая вакансия</h1>

    <%= render "form", job_card: @job_card %>
  </div>
</div>
